# Workflow สำหรับ Claude-Assisted Development (v2)

ขั้นตอนมาตรฐานสำหรับทำงาน project ร่วมกับ Claude — ตั้งแต่เริ่มใหม่ ไปจนถึง iterate ปรับปรุง

---

## โครงสร้างไฟล์

```
project/
├── CLAUDE.md                  ← global instructions สำหรับ Claude (rules, workflow, conventions)
├── .prompts/
│   ├── init.md                ← requirements, schema, architecture, use cases
│   └── improve-*.md           ← improvement requirements (เพิ่มตอน iterate)
└── .breakdown/
    ├── STATUS.md              ← ภาพรวม phase/epic/task ทั้งหมด (kanban board)
    └── [task-name].md         ← รายละเอียดแต่ละ task พร้อม dependencies
```

---

## ภาพรวม Lifecycle

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  1. PLAN    │ ──→ │  2. BUILD   │ ──→ │  3. VERIFY  │ ──→ │  4. IMPROVE │
│  Require    │     │  Breakdown  │     │  Test/Bench  │     │  Analyze    │
│  + Design   │     │  + Implement│     │  + Measure   │     │  + Optimize │
└─────────────┘     └─────────────┘     └─────────────┘     └──────┬──────┘
                                                                    │
                                                              ┌─────▼──────┐
                                                              │ วนกลับ     │
                                                              │ PLAN ใหม่   │
                                                              │ ถ้ายังมีงาน │
                                                              └────────────┘
```

> **สิ่งที่ต่างจาก v1**: workflow เดิมจบที่ BUILD — ไม่มีขั้น VERIFY/IMPROVE  
> ทำให้เมื่อพบปัญหาจาก benchmark/test ต้อง improvise ทุกครั้ง  
> v2 ทำให้ **improvement เป็น first-class citizen** ของ workflow

---

## Phase 1: PLAN

### 1.1 เขียน Requirement

สร้าง `.prompts/init.md` ที่มีอย่างน้อย:
- **Tech stack** — language, framework, external services
- **Scope** — สิ่งที่ระบบต้องทำ (use cases / user stories)
- **Non-goals** — สิ่งที่จงใจไม่ทำใน version นี้
- **Success criteria** — เกณฑ์ที่ชัดเจนว่า "เสร็จ" คือยังไง

เพิ่มเติมตามประเภท project:
| ประเภท | ต้องมีเพิ่ม |
|--------|-----------|
| API/Backend | Database schema, API endpoints |
| Benchmark/Comparison | Scenarios, metrics, fairness rules |
| Frontend | Wireframe, user flow, design tokens |
| Infra/DevOps | Architecture diagram, SLA targets |

> ไม่ต้องสมบูรณ์ 100% แต่ต้องครอบคลุมสิ่งที่อยากได้ — Claude จะถามส่วนที่ขาด

### 1.2 ตั้งค่า CLAUDE.md

```markdown
# CLAUDE.md

## Workflow
- อ่าน .prompts/init.md ก่อนทำงานทุกครั้ง
- อ่าน .breakdown/STATUS.md เพื่อดู context ปัจจุบัน
- Update STATUS.md ทุกครั้งที่ task เปลี่ยน status
- แต่ละ task ต้องมี test เสมอ
- status [DONE] ได้ก็ต่อเมื่อ test ผ่านแล้วเท่านั้น
- Track dependencies ระหว่าง task ทุกครั้ง
```

---

## Phase 2: BUILD

### 2.1 Breakdown ก่อน ห้ามเขียนโค้ด

Prompt:
```
แตก task จาก .prompts/init.md ไปใส่ใน .breakdown/
ยังไม่ต้องเขียนโค้ด ให้ใช้ planning และค้นหาข้อมูลที่จำเป็นก่อน
```

Claude จะสร้าง:
- `.breakdown/STATUS.md` — ตาราง epic/task พร้อม status
- `.breakdown/<task-name>.md` — รายละเอียดแต่ละ task

### 2.2 Review และ Iterate

คุยกับ Claude หลายรอบก่อน approve:
- design choice ที่ยังไม่แน่ใจ
- tech stack ที่มี trade-off
- edge cases ที่อาจพลาด

### 2.3 ทำทีละ Task

```
ทำ task 1.1
```
```
ทำ epic 2
```
```
what's your suggested next task?
```

Claude จะทำทีละก้อน อัปเดต `.breakdown/` ทุกครั้งที่ task เสร็จ

---

## Phase 3: VERIFY

> **ใหม่ใน v2** — ขั้นตอนที่ขาดหายไปใน workflow เดิม

### 3.1 รัน Tests / Benchmarks

หลังจาก BUILD epic ทั้งหมดเสร็จ:
```
รัน test ทั้งหมดและ benchmark
```

### 3.2 วิเคราะห์ผล

```
วิเคราะห์ผลจาก benchmark/results/ — หาสิ่งที่ต้องปรับปรุง
```

Claude จะ:
- วิเคราะห์ผลเทียบกับ success criteria ใน `init.md`
- ระบุปัญหาที่พบ พร้อมจัดลำดับความสำคัญ (🔴/🟡/🟢)
- สร้างรายงานวิเคราะห์

---

## Phase 4: IMPROVE

> **ใหม่ใน v2** — วน loop กลับ PLAN → BUILD สำหรับ improvement tasks

### 4.1 สร้าง Improvement Plan

```
สร้าง improvement plan จากผลวิเคราะห์ เพิ่ม epic ใหม่ใน .breakdown/
```

Claude จะ:
- สร้าง `.prompts/improve-<topic>.md` — เป้าหมายของรอบ improvement
- เพิ่ม epic ใหม่ใน STATUS.md (ต่อจาก epic เดิมที่ DONE ไปแล้ว)
- สร้าง task cards สำหรับแต่ละ improvement item

### 4.2 ทำ Improvement Tasks

เหมือน Phase 2 — ทำทีละ task, test, อัปเดต STATUS.md

### 4.3 Re-verify

กลับไป Phase 3 อีกครั้ง — วัดผลใหม่ เปรียบเทียบ before/after

```
loop จนพอใจ:
  IMPROVE → VERIFY → IMPROVE → VERIFY → ...
```

---

## กฎสำคัญใน CLAUDE.md

```markdown
## Rules

### Testing
- แต่ละ task ต้องมี test เสมอ
- status [DONE] ได้ก็ต่อเมื่อ test ผ่านแล้วเท่านั้น
- ห้าม skip test แม้จะ "simple" task

### Breakdown
- Track dependencies ระหว่าง task ทุกครั้งที่สร้างหรือแก้ task
- Task ที่ blocked ต้องระบุ blocker ชัดเจนใน task card
- Improvement epic ต้องอ้างอิง evidence จากผลวิเคราะห์เสมอ

### Code
- ห้ามเขียนโค้ดก่อนที่ task จะอยู่ใน .breakdown/
- ทำทีละ task อย่าข้ามขั้นตอน
- แต่ละ commit ต้องตรงกับ task เดียว
```

---

## STATUS.md Format

```markdown
# Project Status

> อัปเดตล่าสุด: YYYY-MM-DD | Phase: BUILD / VERIFY / IMPROVE
> Progress: ██████████░░ 75% (9/12 tasks done)

---

## Epic 1: ชื่อ epic
| Task | Description | Status | Priority | Depends On |
|------|-------------|--------|:--------:|------------|
| 1.1 | สร้าง database schema | [DONE] | — | — |
| 1.2 | สร้าง repository layer | [IN PROGRESS] | — | 1.1 |
| 1.3 | สร้าง service layer | [TODO] | — | 1.2 |

## Epic 6: 🔴 Performance Fix (Improvement)
| Task | Description | Status | Priority | Depends On |
|------|-------------|--------|:--------:|------------|
| 6.1 | แก้ broadcast bottleneck | [TODO] | 🔴 | — |
| 6.2 | ลด memory usage | [TODO] | 🟡 | — |

---

## Legend
- [TODO] — ยังไม่เริ่ม
- [IN PROGRESS] — กำลังทำอยู่
- [DONE] — เสร็จแล้ว test ผ่านทั้งหมด
- [BLOCKED] — รอ dependency หรือมีปัญหาต้องแก้ก่อน
- 🔴 Critical  🟡 Medium  🟢 Low
```

**สิ่งที่เพิ่มจาก v1:**
- `Phase` indicator — รู้ว่าอยู่ step ไหนของ lifecycle
- `Progress` bar — เห็นภาพรวมได้ทันที
- `Priority` column — จัดลำดับ improvement tasks
- Improvement epics ใช้ emoji prefix (🔴🟡🟢) แยกจาก build epics

---

## Task Card Format

```markdown
# Task X.Y: ชื่อ task

## Status
[TODO] / [IN PROGRESS] / [DONE] / [BLOCKED]

## Priority
🔴 Critical / 🟡 Medium / 🟢 Low / — (default for build tasks)

## Description
อธิบายว่าต้องทำอะไร

## Evidence (สำหรับ improvement tasks)
- ผลทดสอบที่พบปัญหา: <link to results>
- ค่าปัจจุบัน: X msg/s
- เป้าหมาย: Y msg/s

## Acceptance Criteria
- [ ] เงื่อนไขที่ต้องผ่าน
- [ ] เงื่อนไขที่ต้องผ่าน

## Tests Required
- unit test สำหรับ X
- integration test สำหรับ Y

## Dependencies
- ต้องทำ Task X.0 ก่อน

## Files Affected
- src/module/file.go

## Implementation Notes
(optional — design notes, code snippets, before/after)
```

**สิ่งที่เพิ่มจาก v1:**
- `Priority` — สำหรับจัดลำดับ improvement tasks
- `Evidence` section — **บังคับสำหรับ improvement tasks** ต้องอ้างอิงข้อมูลจริงว่าปัญหาอยู่ตรงไหน ไม่ใช่เดาเอา
- `Implementation Notes` — ใส่ design decision, code diff ที่วางแผนไว้

---

## ตัวอย่าง .prompts/init.md

```markdown
# Project: User Authentication Service

## Tech Stack
- Language: Go 1.25
- Framework: Fiber
- Database: PostgreSQL 16

## Use Cases
1. Register ด้วย email/password
2. Login → ได้ access token + refresh token
3. Refresh access token
4. Logout → revoke refresh token

## Success Criteria
- POST /register → 201 Created ภายใน 200ms
- POST /login → JWT token pair ภายใน 100ms
- 1,000 concurrent logins ไม่ error

## Non-goals v1
- OAuth / Social login
- 2FA / MFA
```

---

## ตัวอย่าง .prompts/improve-*.md

```markdown
# Improvement: Rust Saturation Performance

## Evidence
- Benchmark: benchmark/results/websocket_profile_b_20260228.txt
- ปัญหา: Rust throughput 560 msg/s เทียบกับ Go 2,551 และ Zig 2,951
- Root cause: blocking await ภายใน RwLock ใน broadcast_except()

## Goals
- Rust saturation throughput >= 2,000 msg/s
- Drop rate < 0.5%

## Approach
1. เปลี่ยน broadcast pattern เป็น lock-free fan-out
2. ใช้ AtomicU64 แทน Mutex สำหรับ Stats counters
```

---

## Quick Reference — Prompt ที่ใช้บ่อย

### Phase 1: PLAN
```
เขียน requirements ใน .prompts/init.md สำหรับ [project description]
```

### Phase 2: BUILD
```
แตก task จาก .prompts/init.md ไปใส่ใน .breakdown/
```
```
ทำ task 1.1
```
```
ทำ epic 2
```

### Phase 3: VERIFY
```
รัน test ทั้งหมดและวิเคราะห์ผล
```
```
วิเคราะห์ผล benchmark แล้วหาสิ่งที่ต้องปรับปรุง
```

### Phase 4: IMPROVE
```
สร้าง improvement plan จากผลวิเคราะห์ ค่อยๆทำทีละ task
```
```
แก้ปัญหาทั้งหมด พร้อมทำเอกสาร plan improvement ค่อยๆทำทีละ task
```

### Adjust
```
spin task 1.2 ใหม่และ update dependencies ที่กระทบ
```
```
เพิ่ม improvement epic สำหรับ [ปัญหาที่พบ]
```

---

## ข้อดีของ Workflow นี้

| ปัญหาเดิม | วิธีแก้ใน v1 | ปรับปรุงใน v2 |
|-----------|-------------|--------------|
| Claude เขียนโค้ดแล้วต้องแก้ทั้งหมด | Breakdown ก่อน ไม่เขียนโค้ดจนกว่าจะ approve | เหมือนเดิม ✓ |
| ไม่รู้ว่า Claude ทำถึงไหน | STATUS.md เป็น single source | + Phase indicator + progress bar |
| Task ขาด dependency | Track dependencies ทุก task | เหมือนเดิม ✓ |
| ไม่มี test → bug เยอะ | Test required ก่อน [DONE] | เหมือนเดิม ✓ |
| **ไม่มีขั้น verify ผลลัพธ์** | ❌ ไม่มี | ✅ Phase 3: VERIFY |
| **พบปัญหาแล้วไม่รู้จะ improve ยังไง** | ❌ ต้อง improvise | ✅ Phase 4: IMPROVE + evidence-based tasks |
| **Improvement tasks ไม่มี priority** | ❌ ทำหมดพร้อมกัน | ✅ Priority 🔴🟡🟢 + ทำทีละ task |
| **ไม่มี before/after comparison** | ❌ ไม่ได้วัด | ✅ improve-*.md มี evidence + re-verify |
